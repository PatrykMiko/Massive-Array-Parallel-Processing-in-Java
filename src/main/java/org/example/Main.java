package org.example;

/**
 * Main entry point for the application.
 * Currently left empty, as the core parallel processing examples
 * (Mean and AsyncMean) provide their own main() methods for independent execution.
 */
public class Main {
    public static void main(String[] args) {
        // Entry point - intentionally empty
    }
}